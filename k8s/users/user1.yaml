---
apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  namespace: user1
spec:
  selector:
    app: webapp
  ports:
  - port: 80
    targetPort: 3000

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-ingress
  namespace: user1
spec:
  rules:
  - host: user1.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: webapp-service
            port:
              number: 80